version: "3.7"

volumes:
  boilerplatevolume:
    name: boilerplate-volume

services:
  boilerplate_api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
    ports:
      - 7777:7777
    entrypoint: ["/bin/bash", "./scripts/entrypoint.dev.sh"]